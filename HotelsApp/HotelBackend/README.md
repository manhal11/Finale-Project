### to create a new migration from the models, run in the nuget package console:
```sh
add-migration <migration_name>
```

### Then to update the database from the created migration, run in the nuget package console: 
```sh
update-database
```